name: dopa_app
description: DOPA mobile companion app
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  dio: ^5.7.0
  firebase_core: ^3.5.0
  firebase_messaging: ^15.1.0
  flutter_local_notifications: ^17.2.1
  flutter_secure_storage: ^9.2.2
  file_picker: ^8.1.2
  intl: ^0.19.0

  # Local modules
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
